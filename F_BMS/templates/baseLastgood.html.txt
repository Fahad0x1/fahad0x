<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>{{ title if title else "Unified Platform" }}</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css">
    <style>
        body { padding-top: 60px; }
        .sidebar {
            position: fixed;
            top: 0; left: 0; bottom: 0;
            width: 220px;
            background-color: #d1d7d7d1;
            padding: 20px;
        }
        .content { margin-left: 240px; padding: 20px; }
        .card { margin: 10px; width: 300px; display: inline-block; vertical-align: top; }
        .alarm-box {
            background-color: #f8d7da;
            color: #7b1620;
            border: 2px solid #f5c6cb;
            padding: 15px;
            border-radius: 8px;
            margin-bottom: 20px;
            display: flex;
            align-items: center;
            font-weight: bold;
            font-size: 1.2em;
        }
        .alarm-box.active {
            background-color: #dc3545;
            color: #fff;
            border-color: #b21f2d;
        }
        .alarm-counter {
            background: #fff;
            color: #dc3545;
            border-radius: 50%;
            width: 32px;
            height: 32px;
            display: flex;
            align-items: center;
            justify-content: center;
            margin-left: 15px;
            font-size: 1.1em;
            font-weight: bold;
            border: 2px solid #dc3545;
        }
    </style>
</head>
<body>
<div class="sidebar">
    <h4>Platform</h4>
    <ul class="nav flex-column">
        <li class="nav-item">
            <!-- <a href="{{ url_for('home') }}" class="nav-link {% if request.endpoint == 'home' %}active{% endif %}">💡 Lights</a> -->
                 <a href="{{ url_for('floorplan') }}" class="nav-link {% if request.endpoint == 'floorplan' %}active{% endif %}">🏠 FloorPlan</a>

        </li>
        <li class="nav-item">
            <a href="{{ url_for('users') }}" class="nav-link {% if request.endpoint == 'users' %}active{% endif %}">👥 Users</a>
        </li>
        <li class="nav-item">
            <a href="{{ url_for('smartair_doors') }}" class="nav-link {% if request.endpoint == 'smartair_doors' %}active{% endif %}">🚪 Doors</a>
        </li>
        <li class="nav-item">
            <hr> <!-- خط فاصل -->
        </li>
        <li class="nav-item">
            <a href="{{ url_for('cameras') }}" class="nav-link {% if request.endpoint == 'cameras' %}active{% endif %}">📹 Cameras</a>
        </li>
        
        <li class="nav-item">
            <a href="{{ url_for('settings') }}" class="nav-link {% if request.endpoint == 'settings' %}active{% endif %}">⚙️ Settings</a>
        </li>
        <li class="nav-item">
            <a href="{{ url_for('meetings') }}" class="nav-link {% if request.endpoint == 'meetings' %}active{% endif %}">📋 Meetings</a>
        </li>
        <li class="nav-item">
            <a href="{{ url_for('calendar') }}" class="nav-link {% if request.endpoint == 'calendar' %}active{% endif %}">📆 Calendar</a>
        </li>
        <li class="nav-item mt-3">
            <a href="{{ url_for('logout') }}" class="nav-link text-danger">🚪 Logout</a>
        </li>
        <li class="nav-item">
            <!-- <a href="{{ url_for('floorplan') }}" class="nav-link {% if request.endpoint == 'floorplan' %}active{% endif %}">🏠 FloorPlan</a> -->
        </li>
    </ul>
</div>


    <div class="content">
        {% with messages = get_flashed_messages(with_categories=true) %}
            {% if messages %}
                {% for category, message in messages %}
                    <div class="alert alert-{{ category }} alert-dismissible fade show" role="alert">
                        {{ message }}
                        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                    </div>
                {% endfor %}
            {% endif %}
        {% endwith %}

        {% block content %}{% endblock %}
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>


</html>
